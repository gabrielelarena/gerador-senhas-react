import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  inputer: {
    backgroundColor: '#E5BF3C',
    width: '100%',
    height: 60,
    color: '#000000',
    fontSize: 20,
    borderWidth: 1,
    borderRadius: 5,
    padding: 10,
    textAlign: 'center'
  }
});